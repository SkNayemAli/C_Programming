#include<stdio.h>
int main()
{
	int length,breadth;
	printf("What is the length of the retrangle\n");
	scanf("%d",&length);
	printf("What is the breadth of the retrangle\n");
	scanf("%d", &breadth);
	printf("The area of your rterangle is %d", length*breadth);
	return 0;
	
	

}
